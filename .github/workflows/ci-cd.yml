
name: CI/CD Pipeline

on:
  push:
    branches: [main, dev]
  pull_request:

jobs:
  install-build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          version: 8
      - uses: actions/setup-node@v3
        with:
          node-version: 18
      - run: pnpm install
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm test
      - run: pnpm build

  deploy-frontend:
    needs: install-build-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.Bov5spBKsa2Cm6InPbei5P7m }}
          vercel-org-id: ${{ secrets.team_cAUaKXsvrCDH6TKmEE4vAesA }}
          vercel-project-id: ${{ secrets.prj_IAIVcKZQgIQkfkqzRnXhzOrmoj7z }}
          working-directory: apps/web
          prod: true

  deploy-backend:
    needs: install-build-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy to Render
        run: curl -X POST ${{ secrets.RENDER_DEPLOY_HOOK }}
